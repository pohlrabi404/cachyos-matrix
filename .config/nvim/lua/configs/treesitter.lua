return {
	ensure_installed = { "lua", "luadoc", "python", "markdown-inline" },
	sync_install = true,
	auto_install = true,
	highlight = {
		enable = true,
		use_languagetree = true,
	},
	indent = { enable = true },
}
